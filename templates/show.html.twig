{% extends 'base.html.twig' %}

{% block body %}
    <img src="{{ vich_uploader_asset(image, 'imageFile') | imagine_filter('squared_thumbnail')  }}" />
    <p>
        <a class="bg-black hover-bg-blue white pa2 no-underline dib"
           href="{{ path('image_upload') }}"
        >Back to Image Uploader</a> <a class="bg-black hover-bg-blue white pa2 no-underline dib"
                                       href="{{ path('image_delete', {'id': image.id}) }}"
        >Delete</a>
    </p>
{% endblock %}
